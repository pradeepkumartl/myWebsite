import { LoginValidatorDirective } from './login-validator.directive';

describe('LoginValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
