import { LoginWelcomeDirective } from './login-welcome.directive';

describe('LoginWelcomeDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginWelcomeDirective();
    expect(directive).toBeTruthy();
  });
});
